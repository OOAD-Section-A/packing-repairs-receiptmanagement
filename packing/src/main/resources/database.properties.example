# Copy to database.properties and fill with your local credentials.
# This file is safe to commit.

db.url=jdbc:mysql://localhost:3306/OOAD
db.username=root
db.user=root
db.password=CHANGE_ME
db.pool.size=5
